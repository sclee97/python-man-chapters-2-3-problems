# array of 9 evenly spaced #s going from 0 to 29 inclusive
r = range(0, 30)
b = array(r)

# Finds square of each element in array
b**2

# Finds twice the value of each element in the array, both multiplication and addition
b+b
b*2